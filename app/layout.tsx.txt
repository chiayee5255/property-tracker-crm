import './globals.css';

export const metadata = {
  title: 'Property Tracker CRM',
  description: 'Manage clients, payments, expenses and reports',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
