export default function CustomerDetailPage({ params }: { params: { id: string } }) {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Customer Detail: {params.id}</h1>
      <p>Show client info, payment history, status, etc.</p>
    </div>
  );
}
